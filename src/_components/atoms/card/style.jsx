import styled from 'styled-components'

export const CardContainer = styled.div`
    background-color: var(--white);
    border-radius: 5px;
    padding: 1rem;
    p {
        color: var(--black);
    }
`
