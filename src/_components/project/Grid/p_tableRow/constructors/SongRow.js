export class SongRow {
    constructor(title, arrangement, id, index) {
        this.songId = id
        this.title = title
        this.projectArrangement = arrangement
        this.index = index
    }
}
